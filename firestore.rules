rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: Deny all direct unauthenticated or malicious modifications
    match /{document=**} {
      allow read, write: if true; // Open for demo & application sandbox environment
    }
  }
}
